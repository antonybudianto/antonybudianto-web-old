{
  "name": "antonybudianto-web",
  "version": "0.1.1",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "firebase": "^3.7.5",
    "react": "^15.5.1",
    "react-dom": "^15.5.1",
    "react-router-dom": "^4.0.0",
    "react-snapshot": "^1.0.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:snapshot": "npm run build && react-snapshot",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build:snapshot && firebase deploy"
  }
}
